import React, { Component } from 'react';
import './style.scss';

export default class <%= uppercase_name %> extends Component {
	render() {
		return (
			<div className="<%= lowercase_name %>">
			</div>
		)
	}
}